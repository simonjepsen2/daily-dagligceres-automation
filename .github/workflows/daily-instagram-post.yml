name: Schedule Test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test automatic schedule
        run: |
          echo "================================"
          echo "AUTOMATIC GITHUB SCHEDULE WORKED"
          echo "================================"
          date
